<?xml version="1.0" encoding="utf-8"?>

<resources>
    <style name="CustomScrollbar" parent="android:Widget.ListView">
        <item name="android:scrollbarThumbVertical">@drawable/scrollbar_thumb</item>
        <item name="android:scrollbarTrackVertical">@android:color/transparent</item>
        <item name="android:scrollbarSize">6dp</item>
        <item name="android:fadeScrollbars">false</item> <!-- Disable default fade -->
    </style>


</resources>